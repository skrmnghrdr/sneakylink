// Code the needed #includes here
#include <stdio.h>
#include <limits.h>
// Exercise 1, day 4: find the max and min number of an array of integers
int main( )
{
	int min = ;// The smallest possible integer
	int max = ;// The largest possible integer
	
	int nums[ ] = {-20, 100, 4000, -4500, 125, -999} ;
	
	// Get the number of elements
	int num_elems = ; // The number of elements in the above array
	
	// Code a loop to determine the max and min
	// Print the array, max and min values
	// The line "The min and max of the elements " are printed before the array elements
	
	// Now print the array elements.
	// NO NEWLINE between array elements
	// ALSO, when printing elements note that ALL BUT THE LAST
	// element is followed by a comma and a space
	
	// After printing the elements, print on a NEW LINE
	// the min and max values

			
}
